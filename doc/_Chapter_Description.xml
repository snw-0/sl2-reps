<?xml version="1.0" encoding="UTF-8"?>

<!-- This is an automatically generated file. -->
<Chapter Label="Chapter_Description">
<Heading>Description</Heading>

<P/>
The group <Math>\mathrm{SL}_2(\mathbb{Z})</Math> is generated by <Math>\mathfrak{s}</Math> = <Code>[[0,1],[-1,0]]</Code> and <Math>\mathfrak{t}</Math> = <Code>[[1,1],[0,1]]</Code> (which satisfy the relations <Math>\mathfrak{s}^4 = (\mathfrak{st})^3 = \mathrm{id}</Math>).
Thus, any complex representation <Math>\rho</Math> of <Math>\mathrm{SL}_2(\mathbb{Z})</Math> on <Math>\mathbb{C}^n</Math> (where <Math>n \in \mathbb{Z}^+</Math> is called the <Emph>degree</Emph> of <Math>\rho</Math>) is defined by the <Math>n \times n</Math> matrices <Math>S = \rho(\mathfrak{s})</Math> and <Math>T = \rho(\mathfrak{t})</Math>.
<P/>
This package constructs representations which factor through <Math>\mathrm{SL}_2(\mathbb{Z}/\ell\mathbb{Z})</Math> for some <Math>\ell \in \mathbb{Z}^+</Math>; the smallest such <Math>\ell</Math> is called the <Emph>level</Emph> of the representation. One may equivalently say that the kernel of the representation is a congruence subgroup. It has been shown that any representation arising from a modular tensor category has this property <Cite Key="DLN15"/>.
<P/>
We therefore present representations in the form of a record <Code>rec(S, T, degree, level, name)</Code> where the name follows the conventions of <Cite Key="NW76"/>.
<P/>
Note that our definition of <Math>\mathfrak{s}</Math> follows that of <Cite Key="Nobs1"/>; other authors prefer the inverse, i.e. <Math>\mathfrak{s}</Math> = <Code>[[0,-1],[1,0]]</Code> (under which convention the relations are <Math>\mathfrak{s}^4 = \mathrm{id}</Math>, <Math>(\mathfrak{s}\mathfrak{t})^3 = \mathfrak{s}^2</Math>). When working with that convention, one must invert the <Math>S</Math> matrices output by this package.
<P/>
Throughout, we denote by <Math>\mathbf{e}</Math> the map <Math>k \mapsto e^{2 \pi i k}</Math> (an isomorphism from <Math>\mathbb{Q}/\mathbb{Z}</Math> to the group of finite roots of unity in <Math>\mathbb{C}</Math>).
<P/>
<Section Label="Chapter_Description_Section_Construction">
<Heading>Construction</Heading>

<P/>
Any representation <Math>\rho</Math> of <Math>\mathrm{SL}_2(\mathbb{Z})</Math> can be decomposed into a direct sum of irreducible representations (irreps). Further, if <Math>\rho</Math> has finite level, each irrep can be factorized into a tensor product of irreps whose levels are powers of distinct primes (using the Chinese remainder theorem). Therefore, to characterize all representations of <Math>\mathrm{SL}_2(\mathbb{Z})</Math>, it suffices to consider irreps of <Math>\mathrm{SL}_2(\mathbb{Z}/p^\lambda\mathbb{Z})</Math> for primes <Math>p</Math> and positive integers <Math>\lambda</Math>.
<P/>
Such representations may be constructed using Weil representations as described in <Cite Key="Nobs1" Where="Section 1"/>.  We give a brief summary of the process here.  First, if <Math>M</Math> is any additive abelian group, a <Emph>quadratic form</Emph> on <Math>M</Math> is a map <Math>Q : M \to \mathbb{Q}/\mathbb{Z}</Math> such that <Math>Q(-x) = Q(x)</Math> for all <Math>x \in M</Math> and <Math>B(x,y) = Q(x+y) - Q(x) - Q(y)</Math> defines a <Math>\mathbb{Z}</Math>-bilinear map <Math>M \times M \to \mathbb{Q}/\mathbb{Z}</Math>.
<P/>
Now let <Math>p</Math> be a prime number and <Math>\lambda \in \mathbb{Z}^+</Math>.  Choose a <Math>\mathbb{Z}/p^\lambda\mathbb{Z}</Math>-module <Math>M</Math> of rank 1 or 2 and a quadratic form <Math>Q</Math> on <Math>M</Math> such that the pair <Math>(M,Q)</Math> is of one of the three types described in Section <Ref Sect="Chapter_Description_Section_Weil"/>.  Then the <Emph>quadratic module</Emph> <Math>(M,Q)</Math> gives rise to a representation of <Math>\mathrm{SL}_2(\mathbb{Z}/p^\lambda\mathbb{Z})</Math> on the vector space <Math>V = \mathbb{C}^M</Math> of complex-valued functions on <Math>M</Math>.  This representation is denoted <Math>W(M,Q)</Math>.
<P/>
With a finite number of exceptions, every representation of <Math>\mathrm{SL}_2(\mathbb{Z}/p^\lambda\mathbb{Z})</Math> may be found as a subrepresentation of <Math>W(M,Q)</Math> for an appropriate choice of <Math>(M,Q)</Math> <Cite Key="NW76" Where="Hauptsatz 2"/> (the 18 exceptions can be constructed as the tensor product of two such subrepresentations; these can be generated with <Ref Func="SL2ExceptionalIrreps"/>).
<P/>
The subrepresentations in question are often of the form <Math>W(M,Q,\chi)</Math>, defined as follows.  Let <Math>\mathfrak{A}</Math> be an abelian subgroup of
<Display>\operatorname{Aut}(M,Q) = \{ \varepsilon \in \operatorname{Aut}(M) \mid Q(\varepsilon x) = Q(x) \text{ for all } x \in M\}</Display>
and let <Math>\chi \in \widehat{\mathfrak{A}}</Math> be a 1-dimensional character of <Math>\mathfrak{A}</Math>.  Define
<Display>V_\chi = \{f \in V \mid f(\varepsilon x) = \chi(\varepsilon) f(x) \text{ for all } x \in M \text{ and } \varepsilon \in \mathfrak{A}\}~,</Display>
which is a <Math>SL_2(\mathbb{Z}/p^\lambda\mathbb{Z})</Math>-invariant subspace of <Math>V</Math>.  We then denote by <Math>W(M,Q,\chi)</Math> the subrepresentation of <Math>W(M,Q)</Math> on <Math>V_\chi</Math>.
<P/>
In this context, we will frequently refer to a character <Math>\chi</Math> as being <Emph>primitive</Emph>, which means that there exists some <Math>\varepsilon \in \mathfrak{A}</Math> such that <Math>\chi(\varepsilon) \neq 1</Math> and <Math>\varepsilon</Math> fixes the submodule <Math>pM</Math> pointwise (except for one special case of type <Math>R</Math>; for this, see Section <Ref Sect="Chapter_Description_Section_Weil_Subsection_Type_R"/>).  This amounts to saying that <Math>\chi</Math> does not factor through a quadratic module of lower level.
<P/>
All the irreducible representations of <Math>\mathrm{SL}_2(\mathbb{Z})</Math> of finite level can now be constructed by taking tensor products of these prime-power irreps.  Note that, if two representations are defined by pairs <Code>[S1, T1]</Code> and <Code>[S2, T2]</Code>, then their tensor product may be calculated using the GAP command <Code>KroneckerProduct</Code>, namely as <Code>[KroneckerProduct(S1,S2), KroneckerProduct(T1,T2)]</Code>.
<P/>
</Section>


<Section Label="Chapter_Description_Section_Weil">
<Heading>Weil representation types</Heading>

<P/>
<Subsection Label="Chapter_Description_Section_Weil_Subsection_Type_D">
<Heading>Type D</Heading>

<P/>
Let <Math>p</Math> be prime and <Math>\lambda \geq 1</Math>. Then the Weil representation arising from the quadratic module with <Math>M = \mathbb{Z}/p^\lambda\mathbb{Z} \oplus \mathbb{Z}/p^\lambda\mathbb{Z}</Math> and <Math>Q(x,y) = \frac{xy}{p^\lambda}</Math> is said to be of type <Math>D</Math> and denoted <Math>D(p,\lambda)</Math>.  Information on <Math>(M,Q)</Math> may be obtained via <Ref Func="SL2ModuleD"/>, and subrepresentations of <Math>D(p,\lambda)</Math> with level <Math>p^\lambda</Math> may be constructed via <Ref Func="SL2IrrepD"/>.
<P/>
Here we define
<Display>\mathfrak{A} = (\mathbb{Z}/p^\lambda\mathbb{Z})^\times</Display>
acting on <Math>M</Math> by multiplication; see <Cite Key="NW76" Where="Section 2.1"/>. This group has the following structure. When <Math>p=2</Math> and <Math>\lambda \geq 3</Math>, it is generated by <Math>\alpha = -1</Math> and <Math>\zeta = 5</Math>.
Otherwise, it is cyclic; in this case, we choose a generator <Math>\alpha</Math> and (for simplicity) say <Math>\zeta = 1</Math>.
<P/>
A character of <Math>\mathfrak{A}</Math> is primitive if and only if it is injective on the subgroup <Math>\langle\omicron\rangle \leq \mathfrak{A}</Math>, where <Math>\omicron</Math> is chosen as follows.  If <Math>p=2</Math> and <Math>\lambda \geq 3</Math>, <Math>\omicron = 5</Math>.  If <Math>\lambda = 1</Math>, <Math>\omicron = \alpha</Math>.  In all other cases, <Math>\omicron = 1+p</Math>.
<P/>
</Subsection>


<Subsection Label="Chapter_Description_Section_Weil_Subsection_Type_N">
<Heading>Type N</Heading>

<P/>
Let <Math>p</Math> be prime and <Math>\lambda \geq 1</Math>. Then the Weil representation arising from the quadratic module with <Math>M = \mathbb{Z}/p^\lambda\mathbb{Z} \oplus \mathbb{Z}/p^\lambda\mathbb{Z}</Math> and <Math>Q(x,y) = \frac{x^2 +xy+\frac{1+u}{4}y^2}{p^\lambda}</Math> (where, for <Math>p \neq 2</Math>, <Math>u</Math> is chosen so that <Math>u \equiv 3</Math> mod 4 with <Math>\left(\frac{-u}{p}\right) = -1</Math>, and for <Math>p=2</Math>, <Math>u=3</Math>) is said to be of type <Math>N</Math> and denoted <Math>N(p,\lambda)</Math>.  Information on <Math>(M,Q)</Math> may be obtained via <Ref Func="SL2ModuleN"/>, and subrepresentations of <Math>D(p,\lambda)</Math> with level <Math>p^\lambda</Math> may be constructed via <Ref Func="SL2IrrepN"/>.
<P/>
Here we define
<Display>\mathfrak{A} = \{\varepsilon \in M^\times \mid \operatorname{Nm}(\varepsilon) = 1 \}</Display>
acting on <Math>M</Math> by multiplication; see <Cite Key="NW76" Where="Section 2.2"/>. This group has the following structure. When <Math>\lambda \geq 2</Math>, it is generated by <Math>\alpha</Math> and <Math>\zeta</Math>: for <Math>p=2</Math>, <Math>|\alpha| = 2^{\lambda-2}</Math> and <Math>|\zeta| = 6</Math>, while for <Math>p \neq 2</Math>, <Math>|\alpha| = p^{\lambda-1}</Math> and <Math>|\zeta| = p+1</Math>.  On the other hand, when <Math>\lambda = 1</Math>, <Math>\mathfrak{A}</Math> is cyclic; in this case, we choose a generator <Math>\zeta</Math> with <Math>|\zeta| = p+1</Math> and (for simplicity) say <Math>\alpha = 1</Math>.
<P/>
As a special case, for <Math>p=2</Math>, <Math>\lambda=2</Math>, a character of <Math>\mathfrak{A}</Math> is primitive if and only if <Math>\chi(-1) = -1</Math>.  For all other cases, a character of <Math>\mathfrak{A}</Math> is primitive if and only if it is injective on the subgroup <Math>\langle\omicron\rangle \leq \mathfrak{A}</Math>, where <Math>\omicron</Math> is chosen as follows.  If <Math>\lambda = 1</Math>, <Math>\omicron = \zeta</Math>; otherwise <Math>\omicron = \alpha</Math>.
<P/>
</Subsection>


<Subsection Label="Chapter_Description_Section_Weil_Subsection_Type_R">
<Heading>Type R</Heading>

<P/>
The structure of <Math>(M,Q)</Math> of type <Math>R</Math> depends upon three additional parameters: <Math>\sigma</Math>, <Math>r</Math>, and <Math>t</Math>. The relevant values thereof depend on whether <Math>p=2</Math>, as follows.
<P/>
First, if <Math>p</Math> is an odd prime, let <Math>\lambda \geq 2</Math>, <Math>\sigma \in \{1, \dots, \lambda\}</Math>, and <Math>r,t \in \{1,u\}</Math> with <Math>u</Math> a quadratic non-residue mod <Math>p</Math>.  Then define <Math>M = \mathbb{Z}/p^\lambda\mathbb{Z} \oplus \mathbb{Z}/p^{\lambda-\sigma}\mathbb{Z}</Math> and <Math>Q(x,y) = \frac{r(x^2 + p^\sigma t y^2)}{p^\lambda}</Math>.
<P/>
On the other hand, if <Math>p=2</Math>, let <Math>\lambda \geq 2</Math>, <Math>\sigma \in \{0, \dots, \lambda-2\}</Math> and <Math>r,t \in \{1,3,5,7\}</Math>. Then define <Math>M = \mathbb{Z}/2^{\lambda-1}\mathbb{Z} \oplus \mathbb{Z}/2^{\lambda-\sigma-1}\mathbb{Z}</Math> and <Math>Q(x,y) = \frac{r(x^2 + 2^\sigma t y^2)}{2^\lambda}</Math>.
<P/>
In either case, the resulting representation is said to be of type <Math>R</Math> and denoted <Math>R(p,\lambda,\sigma,r,t)</Math>.  Information on <Math>(M,Q)</Math> may be obtained via <Ref Func="SL2ModuleR"/>, and subrepresentations of <Math>R(p,\lambda,\sigma,r,t)</Math> with level <Math>p^\lambda</Math> may be constructed via <Ref Func="SL2IrrepR"/>.  Note that if <Math>\sigma = \lambda</Math> for <Math>p \neq 2</Math>, then the second factor of <Math>M</Math> is trivial (and hence <Math>t</Math> is irrelevant); this special case is handled by <Ref Func="SL2IrrepRUnary"/>.
</Subsection>


</Section>


</Chapter>

